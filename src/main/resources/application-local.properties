# Local development only. Activate with:
#     ./mvnw spring-boot:run -Dspring-boot.run.profiles=local
#
# These keys are public -- they are in the repository -- so they are safe only because
# this profile is never active in a deployed artifact. Nothing else belongs in this file:
# a setting put here is a setting that silently does not apply in production.
#
# Bracket notation is deliberate. Relaxed binding normalises bare map keys, and these are
# opaque credentials that must bind exactly as written.
webhook-replay.api-keys[sk_test_alice]=11111111-1111-1111-1111-111111111111
webhook-replay.api-keys[sk_test_bob]=22222222-2222-2222-2222-222222222222
